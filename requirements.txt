# Cross-platform dependencies shared by macOS development and Linux/CUDA
# deployment. Install the correct PyTorch build before this file on a GPU server.
-e .

# HKRI Polymer Studio: preprocessing, evaluation, and API.
pandas>=2.0
matplotlib>=3.7
fastapi>=0.110
uvicorn[standard]>=0.27

# GRIN additionally requires torch-scatter. It is deliberately not pinned here:
# its wheel must match the installed PyTorch and CUDA builds. Follow README.md.
